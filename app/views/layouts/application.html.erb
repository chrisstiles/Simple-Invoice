<!DOCTYPE html>
<html>
<head>
  <title>Invoicer</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0, maximum-scale=1.0, user-scalable=no" />
  <%= stylesheet_link_tag    'application', media: 'all', 'data-turbolinks-track' => true %>
  <%= javascript_include_tag 'application', 'data-turbolinks-track' => true %>
  <%= csrf_meta_tags %>
</head>
<body>


<% if flash[:success] %>
	<div class="success">
	  <div class="successtimer"></div>
	  <div class="closesuccess"></div>
	  <span><%= flash[:success] %></span>
	</div>
<% end %>

<div id="pagecontent">

<%= yield %>

</div>

<header>
	<%= link_to image_tag("logo.png", alt: "Invoicer"), invoices_path, class: "logo" %>
	<div id="menu">
		<% if user_signed_in? %>
		<nav>
			<%= link_to "Invoices", invoices_path %>
			<%= link_to "Clients", clients_path %>
		</nav>

		<%= link_to "New Invoice", new_invoice_path, class: "button blue newinvoiceicon" %>

		<div id="sessioninfo">
			
				<%= link_to "Log Out", destroy_user_session_path, :method => :delete %>
				<%= link_to "Profile", settings_path %>
		</div>
		<% else %>
				<div id="sessioninfo">
				<%= link_to "Sign In", new_user_session_path %>
				</div>
		<% end %>	
	</div>	
</header>

</body>
</html>
